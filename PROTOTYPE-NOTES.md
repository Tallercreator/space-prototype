# Прототип «Цифровой оффер» — заметки

Контур кандидата: экран оффера по макету Figma «Цифровой оффер»
(файл `PhH2h4N38MyVQcfEbZDaIw`, узел 188-21317; поведение при скролле — 188-74265).
Собран на `@otp/space-ui-kit`, код — `src/offer/`.

## Сценарии

Переключаются плавающей кнопкой «Сценарии» (правый нижний угол) или хэшем в адресе.
`?clean` в адресе прячет кнопку для сессий с респондентами.
Сценарии «принят» (`#ok`) и «дата подтверждена» (`#dated`) закрыты кодом модератора
**2210** — и в панели, и по прямой ссылке; код запоминается на вкладку (sessionStorage).
Путь кандидата «Принять оффер → дата выхода → принят» кодом не закрыт.

| Хэш | Сценарий |
| --- | --- |
| *(пусто)* | А: оффер активен, до дедлайна 3 дня |
| `#last` | А: последний день ответа |
| `#ok` | А: оффер принят — экран подтверждения с оценкой (Figma 217-96246) |
| `#dated` | А: дата выхода подтверждена, открыта анкета парковки |
| `#gone` | А: срок ответа истёк |
| `#no` | А: кандидат отклонил |
| `#pdf` | Б: конфиденциальная позиция, оффер как PDF-картинка (Figma 188-90576) |
| `#pdf2` | Б, V2: тот же PDF в области фиксированной высоты со скроллом внутри |
| `#pdferr` | Б: PDF не сконвертировался |

Живой поток: «Принять оффер» → экран «Оффер принят, рекрутер свяжется с тобой
для обсуждения даты выхода!». Ниже — карточка оценки 408px (217-101766): вопрос,
«Твоя оценка поможет нам улучшить процесс», 5 звёзд с подписью выбранного значения,
textarea «Поделись мнением», кнопка «Отправить оценку» (активна после выбора звезды).
После отправки — карточка «Спасибо! Оценка отправлена» (221-103690). Форму даты выхода из HTML-черновика убрали
(правка Дарьи 15.09). «Отклонить» → попап-подтверждение → состояние «отклонён». Демо-«сегодня» — 2 сентября 2026 (`DEMO` в `data.ts`).

## Что взято из макета 1:1

- Сетка 1130: баннер 460 (фон — экспорт из Figma без текста, поверх лого/тост/заголовок),
  сайдбар 357 (липкий, наставник и рекрутер прижаты к низу), контент 765 с полями 40.
- Статус-сообщение, плитки дохода (сумма / 137; плитка «Гарант новичка» убрана по правке Дарьи 15.09), формула «из чего складывается»,
  список условий (подпись 257 + значение с иконкой 16), льготы 2×N с 3D-иконками 64,
  блок «Тебя ждёт в ОТП» с тремя рядами карточек (280 / 78 / 129) и декором.
- Панель действий по узлу 205-96208: на всю ширину карточки (поля 24/40, без рамки и
  тени), кнопки 48 «Отклонить» secondary neutral и «Принять оффер» primary neutral
  (чёрная), под ними слева юридическая строка 14px и ссылка «PDF-версия оффера ↗».
- Панель кнопок липнет к низу окна после прокрутки баннера (фреймы 2–3 макета
  «скролл»); наверху страницы не перекрывает условия.

## Пасхалка

Клик по баннеру — залп конфетти из точки клика в брендовых цветах Universal/Brand
(lime, orange, purple, blue-light), `src/offer/confetti.ts`. При `prefers-reduced-motion`
не запускается.

## Расхождения с макетом (осознанные)

- Формула дохода — по узлу 188-29752 (обновлён 15.09): оклад + надбавка за объем
  работ + гарант новичка (лаймовая пометка «на первые 3 месяца работы») × карточки
  коэффициентов «0,03 северная надбавка» и «1,5 региональный коэффициент».
  Значения взяты из макета как есть, в 103 456 ₽ они не сходятся.
- Подпись у «Зарплатной карты» — «Премиальное обслуживание» вместо повторённого
  «Корпоративный тариф»; подпись BestBenefits — про скидки, а не копия «ОТП Коинов».
- Значения «Бессрочный» и «3 месяца» в макете 16px, остальные 14px — в прототипе все 14.
- В заголовке баннера исправлена опечатка «позцию».
- Шаги в сайдбаре: пройденные остаются серыми с номером (как «Анкета кандидата» в
  макете); после принятия добавляются шаги «Дата выхода» и «Парковка» (из HTML-черновика).
- Сценарий Б (188-90576): вместо дохода и условий — рендер PDF на всю ширину
  (`public/offer/pdf-page.webp`, экспорт из макета), дальше «Тебя ждёт в ОТП»; блок
  «Льготы и привилегии» в PDF-сценариях убран по правке Дарьи (15.09). Сам PDF в макете — для другой позиции («Андрей, Финансовый Директор»),
  баннер при этом про Елену; это плейсхолдер из макета, при подготовке теста заменить.
- Экран «оффер принят» по 217-96246: подпись к оценке в макете красная с «(?)»
  (не утверждена) — в прототипе обычным цветом и без знака вопроса; опечатка «Мв»
  исправлена.
- Состояния «последний день», «истёк», «отклонён», ошибка PDF и попап отклонения
  в Figma не нарисованы —
  собраны из компонентов кита по HTML-черновику `offer_candidate_prototype_desktop_1.html`.
- Мобильная раскладка (< 768) — базовая: колонки в столбик, сетки в одну колонку.

## Ассеты

`public/offer/` — WebP из исходников макета (Figma `download_assets`), логотип ОТП — SVG.
Фон баннера `banner-bg.webp` — экспорт временной копии компонента Banner со скрытым
текстом (копия удалена).
